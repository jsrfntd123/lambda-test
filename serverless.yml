service: lambda-test

frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.7

functions:
  hello:
    handler: handler.hello

plugins:
  - serverless-python-requirements


custom:
  pythonRequirements:
#    dockerizePip: true
#    dockerImage: python:3.10
    zip: true